# 本地原型验收报告

日期：2026-07-28  
版本：0.2.0
Hugo：0.162.0 standard / macOS arm64

## 构建结果

- 正式构建成功，不包含草稿。
- 空内容正式构建生成 15 个 HTML 页面。
- 生成目录约 156KB。
- 本机缓存后构建耗时低于 1 秒；完整恢复演练首次构建约 2 秒。
- 正式内容目录已经清空演示文章。
- 三万字和十万字压力稿只在临时测试副本中生成，测试后清理。
- 首页 HTML、CSS、JavaScript和首屏资源总量远低于 1MB。

## 自动检查

- 页面类型、RSS、站点地图、404与搜索索引存在。
- HTML语言为 `zh-CN`。
- 正式构建不包含草稿。
- 内部链接有效。
- 不包含远程CSS、JavaScript、字体与HTTP混合内容。
- 不包含 `.env`、Markdown、部署脚本或Git目录。
- 页面无内联事件处理器。
- 搜索结果使用安全DOM API。
- 文章正文不依赖JavaScript生成。
- 图片生成响应式WebP。

## 浏览器检查

- 桌面首页、文集页、长文页和搜索页已截图。
- 390px视口首页、文集页和长文页已截图。
- 手机页面无页面级横向溢出。
- 长文桌面目录显示，手机目录折叠显示。
- 本地中文搜索返回正确结果。
- 长文正文、目录和阅读进度均存在。

## 恢复演练

本地源码备份通过SHA-256校验，已恢复到全新临时目录，并在恢复目录中重新完成全部构建检查。

## 运行时

正式站只需Nginx静态托管。Hugo与Node只用于构建和检查，均不常驻；不存在数据库或远程运行时依赖。

## 尚待正式部署前完成

- 确认最终站名、域名、作者显示名与首页文案。
- 配置DNS与HTTPS证书。
- 审计服务器现有Nginx默认站点后启用独立配置。
- 写入首篇真实文章并确认最终站名与首页文案。

本轮未连接、修改或重启正式服务器。
