# Word 来稿箱

把待导入的 `.docx` 临时放在此目录，再运行平台的 Word 导入入口。

- `.docx` 和 `.docm` 均被 Git 忽略；宏文档不会导入。
- 文件只作为导入输入，不属于平台源码，也不会进入发布包。
- 私人作者模式下，导入结果写入工作区配置指定的私人内容仓库。
- 演示模式下，导入结果只写入 `.cache/studio-demo-workspace/` 临时副本。
